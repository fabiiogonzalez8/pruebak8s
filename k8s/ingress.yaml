apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-prueba-k8s
spec:
  rules:
  - host: pruebak8s.fabio.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: examen
            port:
              number: 80
